% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11-simulate.R
\name{simulateVariantCellData}
\alias{simulateVariantCellData}
\title{Simulate a cellSNP-lite plus Vireo dataset}
\usage{
simulateVariantCellData(
  path = NULL,
  design = NULL,
  n_cells = 160,
  snps_per_gene = 180,
  genes = NULL,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{path}{Directory to write to. Default: a new session temp directory.}

\item{design}{Data frame with columns \code{sample}, \code{patient} and
\code{condition}. Samples sharing a \code{patient} share both genomes.
Default: five samples across four patients, two of them repeat biopsies of
the same patient.}

\item{n_cells}{Integer. Cells per sample. Default 160.}

\item{snps_per_gene}{Integer. Sites per gene. Default 180.}

\item{genes}{Data frame of genes with \code{gene}, \code{base_depth} and
\code{immune_log2} columns, or NULL for the built-in panel of twelve lung
and immune genes.}

\item{seed}{Integer seed. The draw does not disturb the caller's RNG stream.}

\item{verbose}{Logical. Print progress. Default TRUE.}
}
\value{
A list with \code{root}, \code{cellsnp_paths}, \code{vireo_paths},
\code{vireo_dir}, \code{prefixes}, \code{metadata} (row names are prefixed
cell IDs, ready for \code{addSampleData(data_type = "dataframe")}),
\code{design}, \code{sites}, and \code{truth} - the genome behind each Vireo
label, plus the per-genome genotype matrix.
}
\description{
Writes a complete synthetic multi-genome experiment to disk in exactly the
layout \code{addSampleData()} and \code{inferDonorType()} expect, and returns
the paths and cell metadata needed to load it. Intended for vignettes,
examples, reproducible bug reports, and checking an installation end to end.
}
\details{
Each simulated library contains two genetically distinct individuals. The
first supplies mostly structural cells and the second mostly immune cells,
reproducing the near-total lineage segregation seen in real transplant data -
which is what makes \code{inferDonorType()} work.

Which individual Vireo calls \code{donor0} is randomised per sample, because
that is what Vireo actually does: the labels are arbitrary and are reassigned
on every run. The ground truth is returned in \code{$truth} so a vignette can
show the consequence rather than assert it.

Genotypes are drawn under Hardy-Weinberg from allele frequencies above 0.05,
matching the common-variant panels this package is normally run against, and
per-cell alt counts are binomial with a small sequencing-error rate so that no
site separates perfectly. Read depth carries a per-gene shift between immune
and structural cells, which is the signal \code{findDESNPs()} is designed to
detect.

The output is deliberately denser than a real 10x run - roughly half the
site-by-cell entries are non-zero, against a few percent in practice - so that
a small example still exercises every function.
}
\examples{
sim <- simulateVariantCellData(n_cells = 40, snps_per_gene = 10,
                               verbose = FALSE)
list.files(sim$cellsnp_paths[["S1"]])
head(sim$truth$labels)

}
\seealso{
\code{\link{inferDonorType}} for recovering the donor/recipient mapping,
\code{\link{checkGenotypeConcordance}} for verifying the genotype structure.
}
